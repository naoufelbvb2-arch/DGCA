# الخطوة 12 — أول قياس حقيقي

## قرارات على الخطوة 11

**1. تقسيم الجملة المركّبة — قرارك صحيح.** ترويسة الجملة حلقة وجسمها حلقات مستقلة. وتعليلك يوجبه: ابتلاع الجسم في ترويسته يخلط شرطاً بنتيجته في هوية واحدة، **فيضيع ما وُضعت له ق11**.

**2. `TEXT` معرَّف محلياً — معتمد.** المُرمِّز يتكلّم **صيغة الأسلاك لا بنية الشبكة**، و`quantity` ليست في `REGIONS` أصلاً. العزل البنيوي أثمن من تفادي التكرار.

**3. ملاحظتك عن هيمنة الكلمات المفتاحية — الأهم في تقريرك، وهي نقص في مواصفتي.**

في تصميم المُرمِّز الأصلي كانت خمس مراحل، الرابعة **بوابة التباين**: ما لا يتغيّر يتوقف عن الإبلاغ عنه، استلهاماً من تكيّف المُستقبِل. **أسقطتُها سهواً.** و`kw.assign` في كل حلقة هو **الخلفية الثابتة** التي تُطرح لا إشارة تُرسَل.

> وقولك «العلاج بقانون لا بحاسّة» ليس محسوماً: الشبكية لا ترسل الإضاءة الثابتة إلى الدماغ لينفيها — **بل تطرحها بنفسها**.

**ومع ذلك لن نعالجه الآن.** رصدتَ **فرضية لا نتيجة**، ولم يُقَس بعد: هل تهيمن فعلاً على الأجوبة؟ أم يكبحها سقف الصادر لأنها تنتشر في كل اتجاه فتُقصّ حصصها؟ وهل هيمنتها ضارّة أصلاً — «`if` يُتبع بـ`return`» نمط برمجي **حقيقي**.

**هذه الخطوة تقيس فرضيتك تحديداً.**

---

## المبدأ الحاكم

> **الأرقام تُرصَد ولا تُفرَض.**

هذه الخطوة **أداة قياس لا تحسين**. ولن تحوي **أي عتبة جودة** — لأننا لا نعرفها. ولو وضعتُ عتبة الآن لكنتُ أخمّن، ثم يُجبَر النظام على بلوغ رقم اخترعته.

**كل نتيجة معلومة جديدة مهما كانت.** ونتيجة سيئة أنفع من نتيجة مصنوعة.

## لماذا الآن — لا قبل ولا بعد

بنينا اثني عشر قانوناً و179 اختباراً، **ولم يواجه النظام عالماً لم نصنعه له**. وكل ما نناقشه من توسيعات — تفكير موجَّه، تركيبية، سببية — **تحسينات لمشكلات لم تُقَس**.

وقد اتفقنا: **لا نحسّن ما لم نقسه.**

---

## البنية

### 1. `dgca/metrics.py` — دوال القياس

**الحقيقة الأرضية مجاناً:**

```python
ground_truth(source, module="module") -> {"definitions": [...], "sequences": [...]}
```

- `definitions`: `(head_nid, [param_nids])` من كل حلقة تزامن
- `sequences`: قوائم `nid` من كل تتابع طوله **3 فأكثر** — ليبقى بعد حجب الأخيرة بادئة صالحة

> **وهذه أجمل ميزة في مدونة الكود:** الأجوبة الصحيحة **مستخرَجة من الشجرة النحوية نفسها**. لا وسم يدوي ولا حكم بشري. في النص الطبيعي كنا سنحتاج أسابيع لبناء ما نحصل عليه هنا بسطر.

**المقاييس الخمسة:**

| الدالّة | ما تقيس | المخرَج |
|---|---|---|
| `language_dominance(graph, seeds, k=5)` | نصيب `kw.*` و`op.*` من أعلى k | `share`, `answered`, `asked` |
| `parameter_recall(graph, definitions, k=5)` | بذرة = اسم الدالّة ⟵ هل معاملاتها في أعلى k؟ | `recall`, `hits`, `total`, `answered` |
| `sequence_completion(graph, sequences)` | نحجب الذرّة الأخيرة ونسأل `predict_next` | `accuracy`, `coverage`, `answered`, `asked` |
| `depth_histogram(graph, seeds)` | كم قفزة **مثمرة** يستعمل الاستدلال | `{عمق: عدد}` |
| `growth_snapshot(graph)` | لقطة بنيوية | عقد، روابط، مفاهيم، أصناف، أحداث، مقفلة، مبوَّبة |

**وقاعدة مساعدة:** `is_language_token(nid)` — يعتمد بادئتَي `kw.` و`op.` وحدهما.

### قواعد حاكمة في القياس

**1. فصل الصمت عن الخطأ.** `predict_next` التي تعيد `None` تُحسب في **التغطية** لا في **الدقة**.

> **لماذا:** خلطهما يجعل نظاماً صامتاً يبدو خاطئاً، ونظاماً مختلقاً يبدو أفضل منه. **والصمت فضيلة في نظام معرفة.**

**2. العمق يُقاس بالقفزات المثمرة** — التي نشّطت شيئاً — لا بطول التتبّع.

**3. البذور غير الموجودة تُحصى في `asked` ولا تُحسب في المتوسط.**

**4. كل دالّة تُرجع أرقاماً لا أحكاماً.** لا `bool` ولا «ناجح/فاشل» في أي مخرَج.

### 2. `measure.py` — سكربت التشغيل (ليس اختباراً)

```
python measure.py                 # المدونة الافتراضية
python measure.py --lines 10000   # حدّ الأسطر
```

**المدونة: المكتبة القياسية لبايثون.** متاحة بلا تنزيل، ومتنوعة وواقعية، وبلا اعتماديات.

- اجمع ملفات `.py` من `sysconfig.get_paths()["stdlib"]` **بترتيب أبجدي حتمي**
- تخطَّ ما لا يُحلَّل (`SyntaxError`) وسجّل عددها
- توقّف عند حدّ الأسطر — **افتراضياً 10000**
- كل ملف وحدة (`module`) باسمه

**التغذية:** `feed(graph, source, module)` لكل ملف، مع **لقطة نمو كل 500 حلقة**.

**بذور القياس:** أسماء الدوالّ من الحقيقة الأرضية — أول 200 بترتيب حتمي.

**تقسيم عادل:** آخر **20%** من الملفات **محجوبة عن التغذية** وتُستعمل للقياس وحدها.

> **لماذا الحجب؟** بلا تقسيم نقيس **الحفظ** لا **التعميم**. وهذا الفارق هو كل ما نريد معرفته.

---

## المخرَج المطلوب — وهو غاية الخطوة

**التقرير أهم من الاختبارات.** الاختبارات تثبت أن الأداة تحسب صحيحاً؛ **والتقرير وحده يخبرنا ما حال النموذج.**

### أ. جدول التغذية
ملفات، أسطر، حلقات (تزامن/تتابع)، إشارات، أخطاء تحليل، زمن.

### ب. منحنى النمو
كل 500 حلقة: عقد · روابط · مفاهيم · أصناف · **أحداث** · مقفلة · مبوَّبة.

> **راقب الأحداث خاصةً** — توقّعنا أنها تنفجر تركيبياً، وهذا أول اختبار للفرضية.

### ج. المقاييس الخمسة على الجزء المحجوب

### د. أشيع 20 رمزاً، وتوزيع المناطق

### هـ. **عيّنة يدوية** — عشرة استدلالات مقروءة بعينك
بذرة، وأعلى 5، والمسار، وهل جاءت بالتعميم.

### و. **حكمك أنت** — أربعة أسطر
هل يبدو ما يفعله النموذج معقولاً؟ ما أكثر ما فاجأك؟ ما أسوأ ما رأيت؟ وهل ظهر شيء لم نتوقّعه؟

---

## الاختبارات

`tests/test_step12_metrics.py` — **22 اختباراً**. ضعه كما هو.

> جزء من المواصفة لا من عملك.

تستعمل `StubGraph` صورية بأجوبة مكتوبة سلفاً. **الاختبار هنا يخصّ أداة القياس لا الشبكة** — نتحقق من صحة الحساب، لا من جودة النموذج.

## معايير القبول
1. `bash run_gate.sh` أخضر: **225 اختباراً** (203 + 22)، و`ruff` نظيف.
2. **البصمة `c4b2549940a49789` بلا تغيير.**
3. `measure.py` يعمل ويُخرج التقرير كاملاً.
4. `metrics.py` لا يعدّل الشبكة — قياس بقراءة خالصة.

## محظورات النطاق
- **لا تعديل على `graph.py` ولا `config.py` ولا `encoder.py`.**
- **لا عتبات جودة** في أي دالّة قياس.
- **لا تحسين للنموذج** مهما بدت النتيجة سيئة — هذه خطوة قياس فقط.
- **لا معالجة لهيمنة الكلمات المفتاحية** — نقيسها أولاً.

## المخرج المطلوب
1. `dgca/metrics.py` و`measure.py`.
2. مخرجات `bash run_gate.sh` والبصمة.
3. **التقرير الكامل بأقسامه الستة.**
4. **سطران**: قرار تصميمي لم تحدده المواصفة، وغموض واجهته.

> **إن خرجت الأرقام سيئة، فتلك نتيجة صحيحة لا فشل.** أسوأ ما يمكن أن يحدث في هذه الخطوة هو أرقام جميلة لا نثق بها.
